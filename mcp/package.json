{
  "name": "trace-lab-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP bridge: lets an MCP client spectate the trace-lab TUI and query the lens engine",
  "scripts": {
    "start": "bun run server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
